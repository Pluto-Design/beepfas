---

const { title, text, icon } = Astro.props;

---

<div class="w-full min-h-[20rem] max-2xl:py-8 2xl:w-1/3 h-full flex flex-col items-center justify-start border-x-[2rem] border-bright-red">
    <div class="w-24 h-24 rounded-xl -translate-y-12 shadow-2xl pointer-events-none">
        <img src={icon} alt="" class="w-full h-full object-cover rounded-xl p-4 pointer-events-none">
    </div>

    <h3 class="text-bright-red text-xl md:text-3xl font-bold">{title}</h3>

    <p class="w-full p-8 text-lg text-black">
        {text}
    </p>
</div>